type AuthenticatedUser {
  """
  User authentication token based on JWT
  """
  token: String
}

type Mutation {
  login(username: String!, password: String!): AuthenticatedUser!
  # logout: String!
}