type Owner {
  id: String!
  name: String!
  surname: String!
  avatar: String
}

type PostComment {
  body: String!
  owner: PostOwner! # ❌ It would fail 'cos doesn't exist the type
  owner: PostOwner! # ❌ It would fail 'cos the field is duplicated
}